apiVersion: v1
kind: Service
metadata:
  name: wiz-app-service
  namespace: wiz-app
  labels:
    app: wiz-app 
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: wiz-app