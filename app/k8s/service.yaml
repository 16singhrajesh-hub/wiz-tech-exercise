apiVersion: v1
kind: Service
metadata:
  name: todo-app-service
  namespace: wiz-app
  labels:
    app: todo-app 
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: todo-app